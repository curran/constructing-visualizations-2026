Scatter plot basic looks pretty good. I like the way that use scales is structured. We pass in the data and the width and height from dimensions. It would be slightly more concise to use the rest spread operator with dimensions: ...dimensions. And you know, the x value and y value accessors, I want to structure it so that those are defined inside of ScatterPlotBasic instead of use scales. Let's take a look at what use scales is doing. use scales options looks good. We have the data, the width and the height. The scales interface really should just be returning x scale and y scale, although it does define x value and y value correctly conceptually, but those should be passed in from the outside rather than defined in here. And then the function itself leverages React's use memo, so it memoizes the scales. This is perfect. Scales are a perfect example of a value that should be memoized like this. And the dependencies are the data, the width and the height. So every time the screen resizes, it will recompute the scales, which is correct. x value and y value are defined correctly as functions that take as input a penguin row and return some property of it. These look good. It should just be defined on the outer level. And then we compute the extent, which is a utility from D3 that computes the min and max of the data across a certain function. Now it's falling back to empty array. Ideally, we could simplify this and just pass in the data. But actually, you know, let's not do that. I kind of like this because data could be null. That's the thing. If the data is loading, this whole thing is null. And actually, I would prefer to do it in a slightly different way, where we have a guard inside the use memo. Like, if there is no data, then return whatever the empty representation is for the scales. Maybe the cleanest way to do it is so that they could be null, so that that idea of null propagates all the way through until the data is loaded. That should let us remove this comment that says D3 extent skips missing values, and empty result means no usable data yet. We shouldn't even get to this code. Like, we should not even try to construct the scales if there's no data. But otherwise, the definition of X scale and Y scale are correct here. We're using a scale linear, which does linear interpolation with D3, and then just passing in x domain, which really should just be whatever is returned from extent. I would love to see this extent call within what we're passing into domain, just to simplify it. And the range is the screen space. So let me take a step back and explain about scales a little bit. D3 scales is what maps the data space to the screen space, and there's this idea of a domain and a range. The domain are the data values. It's a thing that represents the space of possible values from the data set. And then the range, in this case, we're projecting it onto the screen onto the x coordinates, going from left to right. And so the range just goes from 0 to width, which is our measured div width. And similarly for the Y scale, except for the height is flipped, because larger values appear higher on the screen, and in SVG coordinates the 0 is at the top. So that looks pretty good for use scales. We just want to define the x and y value outside of there and simplify how we handle the case where the data is still loading. Now back in ScatterPlotBasic, after we call use scales, there is this use effect that calls render circles. Let's take a look at that. Okay, so we have a hardcoded radius of 3. That looks good. We have the options defined here. That looks correct. The only stuff that we need to render these circles are the data, the x and y scales, and then the value accessors. Now the concept of a value accessor is sort of universal with D3 scales. It's a function that takes as input a row from your data set and returns a value, which can be part of the domain of the scale. So the render circles function itself is super simple. This is what I like to see. Yeah, no refinements needed here. We are just destructuring the options and then having this nice concise D3 method chaining thing going on, where we're selecting all the circles, passing in our data, doing the data join to create the circles, one for each row, and then using this functional syntax to set the center x coordinate to be the x value of the row, passed through the x scale. Now one minor nitpick: I kind of want to call row D, just the letter D, just because that's sort of a convention in the D3 world.