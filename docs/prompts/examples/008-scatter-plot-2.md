One thing I would like to change is margins should be just margin with no S. Just because that's convention in the D3 world. I like that we have a function now called renderAxes, which follows the similar pattern to the other one, renderCircles. We destructure the options. Perfect. We have the data join pattern with classes. I think this is the first time we're seeing this, so let me just stop and explain. When you have two sibling DOM elements at the same level, they're often disambiguated using classes. So this .x-axis is great because, you know, we can then have multiple group elements at the same level of the DOM and be able to, you know, look at them independently. Let me show you what I mean by that by inspecting the DOM. And you can do this if you right-click and say Inspect. So there's the top-level SVG, and within that, there's circles, text elements, and then these two g elements, which are SVG group elements, one for the X axis, one for the Y axis. See how you can hover on them here. And then there's a domain path. That's the line of the axis, and then there are tick elements, and within each tick, there is a line, which is that little tick mark, and the text. This is what D3 axis generates for us. So this implementation actually looks great for renderAxes. And when you have a group element, you can apply the transform property, which is a domain-specific language where you can specify a translation. So this means move the whole group element, which is a container for everything inside of it, down by translating it by zero in the X direction and in the Y direction, height minus margins.bottom. That should become margin.bottom, which positions it in the right spot. And then we use .call, which is a utility on D3 selections where you can pass into it a function that accepts as the first argument the selection itself. So it's a way to chain function calls, but it's a pretty clean construct. So we are actually creating a new instance of axisBottom, passing in the X scale, and then calling that as a function, passing in our selection of the group element. So that's how it's working. And same pattern for Y. Very nice. renderCircles remains the same. And now we have another thing called renderLabels. One thing I would like to change is that the axis label offset should not be a constant defined in this file, because this is one of those things that we may want to tweak. Instead, that should be part of the renderLabels options, and often we want to tweak them separately for X and Y. So let's have two different options: renderLabelOptions.xAxisLabelOffset and YAxisLabelOffset. But anyway, let's take a look at renderLabels. We compute plotCenterX and plotCenterY. So for X, we start with the left margin, and we add to that half of width minus both margins. And so that gets us to the center point of that space between the minimum and maximum of X. And same for Y. This is where the labels go. I love how we use the data join pattern for singular elements, and also we use class to disambiguate between the various text elements. So this is the one for the title, and this positions it in the center horizontally, and then in the center of the space of the margin on the top. I like that. And text anchor middle is just one of those SVG text things that you need to use to center it. I also don't like how font size and the actual title text are hardcoded. Those should both be also options within renderLabels options. And then for the axis labels themselves, it's a very similar pattern. I also don't like how the font size and text for the axis labels is hardcoded here. Those should also be on the renderLabels options. Maybe let's call them axisLabelFontSize, and that should be the same across X and Y, and then XAxisLabel and YAxisLabel. And see here, if you want rotated text, you can use transform, translate, and then rotate. See this is that domain-specific language where you can have a translate followed by a rotate, or a rotate followed by a translate. But anyway, that looks good. And finally in the top-level component, ScatterPlot.tsx, we are importing the various things, and here we define xValue and yValue. Actually in this part of the code, right below xValue and yValue, this is where I would like to see margin defined and then passed in wherever it needs to be accessed. And also all the various tweakable things should be defined here in one big bunch and then passed in to the various components as needed. So now useScales takes as input the margins as well. So let's take a look at that too. But anyway, this looks good. We have one useEffect that renders the circles and a separate useEffect that calls renderAxes. Okay. And another useEffect that calls renderLabels. I guess this makes sense, but there's kind of a bunch of duplication across these effects. But I guess it does make sense actually, because they have different dependencies. But still, my preference is to sort of consolidate these. So let's actually make it so that there's just one useEffect that gets access to the SVG, and then it should call renderCircles, renderAxes, and renderLabels just to clean it up and reduce duplication. And finally useScales. Let's see how this is different. This is the main definition of the scales.

All right, so for use scales, the domain is unchanged from the previous example, but the range, which defines the screen coordinates of where we're mapping it to, now takes into account the margins. So it's pretty clean. We just have the range start at the left margin and go to width minus the right margin, and a similar thing for y. But one modification I would like to do here, just for code aesthetics, let's define x scale and y scale as variables in this function, and then return the object that just combines them together, just so we don't have one massive object literal statement that has a bunch of complexity.