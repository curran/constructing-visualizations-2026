In `src/examples/006-loading-and-summarizing-data/LoadingAndSummarizingData.tsx`:

What I don't like about the loading and summarizing data example is that it's too monolithic. There's just one big file, and this is a great opportunity to introduce the idea of refactoring into custom React hooks, so that we have the different pieces of logic isolated into separate files that we can easily look at in isolation. There is this useEffect that fetches from the data URL and sets the state, and let's encapsulate that into a separate file called usePenguinsDataset. And also make the following changes. We are using fetch and then response.text, but then, you know, we don't really need that, because D3 has a very nice d3.csv utility that we can import, and we may need to install the package called d3-fetch for this, but I would like to see this promises chain with fetch replaced with a simpler version, which would be using d3.csv and passing as the second argument to that function a function that parses one single row. And I really don't like the idea of minting new objects for each row due to performance for large datasets in the future. So instead what we should do is mutate that row and return it, because that's more efficient and idiomatic with D3. Okay, moving on to the summary, that should also be a custom hook called useRowsAndColumnsSummary. And we want to keep that console log just for future reference. And this last useEffect that is responsible for rendering, I would like this useEffect to stay in this file, but it should call out to a function that just contains the D3 logic in isolation, and that should be called renderTextSummary. And it should be structured as follows. It should be a function that takes as input, the first argument should be called selection, which would be our SVG D3 selection, and then the second argument should be an options object that has centerX, centerY, and something called label. And I don't want this to be two separate text elements with the tspan because that's overly complex for this example. So make it just a string that says rows, colon number of rows, comma columns, colon number of columns, and that should just be one line. So I'm changing the behavior here just to simplify this example. That should be a good cleanup.