So we have this progression where we have the first example is a pseudo scatter plot, and then it leads up to this responsive pseudo scatter plot, and then we have this loading and summarizing data. Now this is all building towards what we're going to do next, which is create the next example in the series called Scatterplot Basic, and this will be a basic version of a scatter plot where we should start by copying example six, the loading and summarizing data example, and that's well organized already in terms of the hooks. We have hooks set up for use dimensions and use penguins dataset. Those should stay, but we should remove the use rows and columns summary hook and instead replace that with a new custom hook. I think a custom hook is a great way to organize the setup for the D3 scales. So it should be use scales, and that should return an object that we can deconstruct, or de-structure rather, into two variables called x scale and y scale. Those should be linear scales, and we should have accessor functions x value and y value. And then the use effect that currently renders the text summary should call render circles, and it should pass in everything required to render the circles of a scatter plot. So each of the rows from the penguins dataset should correspond to one circle on the screen, just a simple black circle, and no margins for now, no axes, no labels. We just want to show the simple core essence of using D3 scales to map data rows to circles on the screen. Make these circles pretty small, maybe a radius of three.