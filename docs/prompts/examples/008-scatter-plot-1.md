Okay, we have our basic scatter plot, this example called Scatter Plot Basic. The next logical step is to create the next example in the series, which is just Scatter Plot. And in this example, we should introduce margins and axes, including axis labels that are centered on the axes. So the X-axis label should be centered on the X-axis and the Y-axis label should be centered on the Y-axis, but rotated text. Let's also include a title at the top with SVG text, just for completeness. The title should be Palmer Penguins.